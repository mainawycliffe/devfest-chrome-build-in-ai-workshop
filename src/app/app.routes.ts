import { Routes } from '@angular/router';
import { TextImproverComponent } from './components/text-improver/text-improver.component';

export const routes: Routes = [
  { path: '', component: TextImproverComponent }
];
